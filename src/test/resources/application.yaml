spring:
  datasource:
    url: jdbc:h2:mem:test
    username:
    password:
    driver-class-name: org.h2.Driver

  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: none
        format_sql: true
        show_sql: true
